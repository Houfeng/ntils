build:
  - name: shell
    script:
      - rollup -i src/index.js -o lib/index.js -f cjs -n ${project.name}
      - rollup -i src/index.js -o dist/${project.name}.js -f umd -n ${project.name}

dev:
  - name: shell
    script:
      - dn build